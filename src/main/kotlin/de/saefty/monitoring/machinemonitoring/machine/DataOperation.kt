package de.saefty.monitoring.machinemonitoring.machine

/**
 * Describes possible aggregation and segregation operations on the underlying data.
 */
enum class DataOperation {
    MEDIAN,
    MEAN,
    MIN,
    MAX
}
